#include <stdio.h>

int main() {
    int a;
    printf("enter the number :");
    scanf("%d",&a);
     int b;
    printf("enter the number: ");
    scanf("%d", &b);

    while (a <= b)
     {
        if (a % 2 != 0)
        {
            printf("%d",a);
        }
        a++;
    }

    return 0;
}

#include <stdio.h>

int main() {
    int num, i = 1, fact = 1;

    printf("Enter a integer: ");
    scanf("%d", &num);

    while (i <= num) {
        fact *= i;
        i++;
    }

    printf("Factorial of %d = %d\n", num, fact);

    return 0;
}
